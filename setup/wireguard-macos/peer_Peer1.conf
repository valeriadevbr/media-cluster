[Interface]
Address = ${WG_PEER2_IP}
PrivateKey = ${WG_PEER2_PRIVATE_KEY}
ListenPort = ${WG_SERVER_PORT}
DNS = ${WG_SERVER_IP}
MTU = ${WG_MTU}

[Peer]
PublicKey = ${WG_SERVER_PUBLIC_KEY}
PresharedKey = ${WG_PEER2_PRESHARED_KEY}
Endpoint = ${WAN_HOSTNAME}:${WG_SERVER_PORT}
AllowedIPs = ${WG_SUBNET}, ${DOCKER_HOST_SUBNET}
